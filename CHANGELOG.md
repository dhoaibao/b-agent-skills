# Changelog

## Next - Claude Code vNext

- Hard-cut runtime boundary to Claude Code.
- Added the Claude-native plugin manifest, routed skills, custom planner/worker agents, direct MCP config, fail-closed permission hook, status hook, and settings denials.
- Replaced runtime installation with preserving Claude configuration/plugin lifecycle; Claude Code itself is never auto-updated.
- Added generated plugin synchronization, managed-MCP policy fixtures, hook decision fixtures, isolated install/uninstall smoke coverage, solo routing validation, and named-session messaging checks.
