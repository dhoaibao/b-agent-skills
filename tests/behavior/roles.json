{
  "version": 2,
  "source": "b-agentic Claude Code named-session workflow scenarios",
  "scenarios": [
    {
      "id": "planner-worker-boundary",
      "prompt": "The planner must remain read-only while the named worker owns all edits.",
      "expected": ["b-planner", "b-worker", "ListAgents", "SendMessage"]
    },
    {
      "id": "fresh-peer-discovery",
      "prompt": "Before a handoff, discover the current named peer rather than using a stale target.",
      "expected": ["ListAgents", "SendMessage"]
    },
    {
      "id": "terminal-result",
      "prompt": "The worker reports changed paths, exact checks, gaps, and requests actual review before pausing.",
      "expected": ["b-review", "sole worktree writer"]
    }
  ]
}
